# dex_platform/configs/backtests/weth_usdc_030_mar2026_proxy.yaml
#
# Documentation config for the first LP proxy test.
# The current runner uses CLI args. Keep this file as a reproducibility note.

dataset:
  events: DEX_DATA/uniswap_v3_weth_usdc_030_2026_03/events_all.csv
  pool: WETH/USDC
  fee_tier_bps: 30

capital:
  initial_capital_usd: 10000

fee_model:
  active_liquidity_usd_proxy: 10000000
  reference_width_pct: 40
  min_concentration_boost: 0.1
  max_concentration_boost: 50

wide_benchmark:
  lower_pct: 25
  upper_pct: 18

adaptive:
  inner_width_pct: 1.0
  outer_offset_pct: 0.5
  outer_width_pct: 1.5
  inner_capital_pct: 40
  lower_capital_pct: 25
  upper_capital_pct: 25
  rebalance_hours: 12
  gas_per_rebalance_usd: 25
  rebalance_swap_cost_bps: 5

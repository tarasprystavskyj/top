import useSWR from 'swr'; import Link from 'next/link'; import { apiFetch } from '../../utils/api'; const f=(u:string)=>apiFetch(u).then(r=>r.json()); export default function P(){const {data}=useSWR('/api/configs',f); if(!data) return 'Loading...'; return <ul>{data.map((c:any)=>(<li key={c.name}><Link href={'/configs/'+c.name}>{c.name}</Link></li>))}</ul>}

# ═══════════════════════════════════════════════════════════════════════════════
# PATCH для UI/backend/api_main.py
# ═══════════════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────────────
# КРОК 1: Додай імпорт dotenv на початку файлу (після інших імпортів, ~рядок 20)
# ───────────────────────────────────────────────────────────────────────────────

# ЗНАЙДИ блок імпортів (близько рядка 1-20), ДОДАЙ ПІСЛЯ ОСТАННЬОГО import:

from dotenv import load_dotenv

# Завантажуємо змінні з .env
load_dotenv()


# ───────────────────────────────────────────────────────────────────────────────
# КРОК 2: Інтегруй Git Deploy router (~рядок 1055)
# ───────────────────────────────────────────────────────────────────────────────

# ЗНАЙДИ рядок:
app = FastAPI()

# ДОДАЙ ВІДРАЗУ ПІСЛЯ НЬОГО (ці 3 рядки):

# Git Deploy endpoints
from git_deploy import router as deploy_router
app.include_router(deploy_router, prefix="/api", tags=["deploy"])


# ───────────────────────────────────────────────────────────────────────────────
# Результат повинен виглядати так:
# ───────────────────────────────────────────────────────────────────────────────

app = FastAPI()

# Git Deploy endpoints
from git_deploy import router as deploy_router
app.include_router(deploy_router, prefix="/api", tags=["deploy"])

@app.get("/api/health")
def health(): return {"ok": True}


# ═══════════════════════════════════════════════════════════════════════════════
# КІНЕЦЬ ПАТЧА
# ═══════════════════════════════════════════════════════════════════════════════
